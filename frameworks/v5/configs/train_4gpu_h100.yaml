extends: base_3gpu.yaml

run:
  output_dir: ../runs/dual_recursive_4gpu_h100

distributed:
  enabled: true
  world_size: 4
  backend: nccl

captioner:
  generation:
    rollout_batch_size: 1
  training:
    replay_batch_size: 1
